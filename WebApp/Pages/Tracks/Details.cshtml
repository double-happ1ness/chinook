@page
@model Project.Pages.Tracks.DetailsModel

@{
    ViewData["Name"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Track</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Track.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Track.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Track.TrackId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Track.TrackId)
        </dd>
        <dd class="col-sm-10">
            @* <table class="table">
                <tr>
                    <th>Course Name</th>
                    <th>Grade</th>
                </tr>
                @foreach (var item in Model.Track.Artist)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ArtistId)
                        </td>
                    </tr>
                }
            </table> *@
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Track.TrackId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
