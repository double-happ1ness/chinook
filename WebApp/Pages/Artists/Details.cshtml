@page
@model Project.Pages.Artists.DetailsModel

@{
    ViewData["Name"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Artist</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Artist.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Artist.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Artist.ArtistId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Artist.ArtistId)
        </dd>
        <dd class="col-sm-10">
            @* <table class="table">
                <tr>
                    <th>Course Name</th>
                    <th>Grade</th>
                </tr>
                @foreach (var item in Model.Artist.Artist)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ArtistId)
                        </td>
                    </tr>
                }
            </table> *@
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Artist.ArtistId">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
